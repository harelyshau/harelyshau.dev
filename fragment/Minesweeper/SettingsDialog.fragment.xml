<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
    core:require="{
        formatter: 'pharelyshau/model/formatter',
        formatMessage: 'sap/base/strings/formatMessage'
    }">
    <Dialog title="Set up field" icon="sap-icon://table-view"
        afterClose="onAfterCloseRecordsDialog" binding="{/Level}">
        <HBox class="sapUiSmallMargin" justifyContent="SpaceBetween" id="settingsBox">
            <VBox>
                <Label text="Width" showColon="true" design="Bold"/>
                <Input width="5em" value="{path: 'Width', type: 'sap.ui.model.type.Integer' }"
                    valueState="{= ${Width} > 100 || 1 > ${Width} ? 'Error' : 'None' }"
                    valueStateText="{= ${Width} > 100 ? 'Value must be less than or equal to 100' : 1 > ${Width} ? 'Value must be greater than 0' : '' }"/>
            </VBox>
            <VBox>
                <Label text="Height" showColon="true" design="Bold"/>
                <Input width="5em" value="{path: 'Height', type: 'sap.ui.model.type.Integer' }"
                    valueState="{= ${Height} > 100 || 1 > ${Height} ? 'Error' : 'None' }"
                    valueStateText="{= ${Height} > 100 ? 'Value must be less than or equal to 100' : 1 > ${Height} ? 'Value must be greater than 0' : '' }"/>
            </VBox>
            <VBox>
                <Label text="Mines" showColon="true" design="Bold"/>
                <Input width="5em" value="{path: 'Mines', type: 'sap.ui.model.type.Integer' }"/>
            </VBox>
        </HBox>
        <beginButton>
            <Button text="Apply" type="Emphasized" press="onPressApplySettings"/>
        </beginButton>
        <endButton>
            <Button text="{i18n>btnClose}" press="onPressCloseModalWindow"/>
        </endButton>
    </Dialog>
</core:FragmentDefinition>