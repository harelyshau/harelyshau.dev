<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
    core:require="{formatter: 'pharelyshau/model/formatter'}">
    <Dialog title="You dealt with Tower of {/DiscCount} Disks" state="Success" afterClose="onAfterCloseWinDialog">
        <VBox class="sapUiSmallMargin">
            <!-- Result -->
            <VBox renderType="Bare">
                <Title text="Your Result:" titleStyle="H6"/>
                <HBox class="sapUiTinyMarginTop">
                    <!-- Time -->
                    <HBox renderType="Bare" alignItems="Center">
                        <core:Icon src="sap-icon://fob-watch"/>
                        <Text text="Time: {path: '/Time', formatter: 'formatter.timeMinSec'}" class="sapUiTinyMarginBegin"/>
                    </HBox>
                    <!-- Moves -->
                    <HBox renderType="Bare" alignItems="Center" class="sapUiSmallMarginBegin"
                        tooltip="{= ${/Moves} === Math.pow(2, ${/DiscCount}) - 1 ? 'You did it in the minimum number of moves' : '' }">
                        <core:Icon src="sap-icon://step"
                            color="{= ${/Moves} === Math.pow(2, ${/DiscCount}) - 1 ? 'Positive' : '' }"/>
                        <Text text="Moves: {/Moves}" class="sapUiTinyMarginBegin"/>
                    </HBox>
                </HBox>
            </VBox>
            <!-- New Record -->
            <VBox class="sapUiSmallMarginTop" renderType="Bare" visible="{= !!${view>/previousRecord}
                &amp;&amp; (${view>/previousRecord/Time} > ${/Time} || ${view>/previousRecord/Moves} > ${/Moves}) }">
                <Title text="New Record:" titleStyle="H6"/>
                <HBox class="sapUiTinyMarginTop">
                    <!-- Time -->
                    <HBox visible="{= ${view>/previousRecord/Time} > ${/Time} }"
                        renderType="Bare" alignItems="Center" class="sapUiSmallMarginEnd">
                        <core:Icon src="sap-icon://fob-watch" class="sapUiTinyMarginEnd"/>
                        <Text text="{= ${view>/previousRecord/Time} - ${/Time} } sec faster"/>
                    </HBox>
                    <!-- Moves -->
                    <HBox visible="{= ${view>/previousRecord/Moves} > ${/Moves} }"
                        renderType="Bare" alignItems="Center">
                        <core:Icon src="sap-icon://step" class="sapUiTinyMarginEnd"/>
                        <Text text="{= ${view>/previousRecord/Moves} - ${/Moves} } moves fewer"/>
                    </HBox>
                </HBox>
                <Button text="See Your Records" icon="sap-icon://competitor"
                    press="onPressOpenRecordsDialog" class="sapUiTinyMarginTop"/>
            </VBox>
        </VBox>
        <beginButton>
            <Button text="Level UP" press="onPressLevelUp" icon="sap-icon://collapse-group" type="Emphasized"/>
        </beginButton>
        <endButton>
            <Button text="Restart" icon="sap-icon://restart" press="onPressCloseWinDialog"/>
        </endButton>
    </Dialog>
</core:FragmentDefinition>
