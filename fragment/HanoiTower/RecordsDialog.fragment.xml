<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
    core:require="{formatter: 'pharelyshau/model/formatter'}">
    <Dialog title="{i18n>ttlYourRecords}" icon="{i18n>iAchievement}" afterClose="onAfterCloseRecordsDialog"
        class="phRecordsDialog">
        <List items="{/Records}" visible="{= ${/Records}.length > 0}">
            <CustomListItem class="phNoFocus">
                <HBox wrap="Wrap" alignItems="Center" class="sapUiSmallMargin" justifyContent="SpaceBetween">
                    <VBox class="sapUiSmallMarginEnd" renderType="Bare">
                        <Title text="Tower of {DiscCount} Discs" titleStyle="H6"/>
                        <HBox renderType="Bare" alignItems="Center" class="sapUiTinyMarginTop">
                            <!-- Time -->
                            <HBox renderType="Bare" alignItems="Center">
                                <core:Icon src="{i18n>iTime}"/>
                                <Text text="{i18n>ttlTime}: {path: 'Time', formatter: 'formatter.timeMinSec'}" class="sapUiTinyMarginBegin"/>
                            </HBox>
                            <!-- Moves -->
                            <HBox alignItems="Center" renderType="Bare" class="sapUiSmallMarginBegin"
                                tooltip="{= ${Moves} === 2 ** ${DiscCount} - 1 ? 'You did it in the minimum number of moves' : '' }">
                                <core:Icon src="{i18n>iStep}"
                                    color="{= ${Moves} === 2 ** ${DiscCount} - 1 ? 'Positive' : '' }"/>
                                <Text text="{i18n>ttlMoves}: {Moves}" class="sapUiTinyMarginBegin"/>
                            </HBox>
                        </HBox>
                    </VBox>
                    <Button icon="{i18n>iImprove}" tooltip="{i18n>btnTryToImprove}" press="onPressImrpoveResult"/>
                </HBox>
            </CustomListItem>
        </List>
        <IllustratedMessage visible="{= ${/Records}.length === 0}" illustrationSize="Spot"
            title="{i18n>ttlNoRecords}" illustrationType="sapIllus-SuccessBalloon"
            description="{i18n>tCompleteToSeeRecord}"/>
        <endButton>
            <Button text="{i18n>btnClose}" press="onPressCloseRecordsDialog"/>
        </endButton>
    </Dialog>
</core:FragmentDefinition>